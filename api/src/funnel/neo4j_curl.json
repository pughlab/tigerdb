{
  "name": "neo4j curl",
  "executors": [{
    "image": "registry.gitlab.com/childdb/imic/alpine-curl-openssl",
    "command": ["sh", "-c", "curl -u neo4j:letmein -H \"Content-Type: application/json\" -X POST -d '{\"statements\" : [ {\"statement\" : \"MATCH (n) RETURN n LIMIT 25\"} ]}' http://YOURIP:7474/db/neo4j/tx/commit"],
    "stdout": "/outputs/hello-out.txt"
  }]
}