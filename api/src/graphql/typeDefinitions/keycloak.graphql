scalar Email

type KeycloakUser {
  keycloakUserID: ID!
  name: String!
  email: Email!
  # roles: [String]
}

input KeycloakUserInput {
  keycloakUserID: ID!
}

type Mutation {
    me: KeycloakUser @auth
}

extend type KeycloakUser @auth(
  rules: [{
      isAuthenticated: true,
    }]
)